from __future__ import annotations

from typing import Any


def enrich_papers_with_crossref(
    papers: list[dict[str, Any]],
    mailto: str | None = None,
) -> list[dict[str, Any]]:
    return papers
